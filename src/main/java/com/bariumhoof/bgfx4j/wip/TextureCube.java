package com.bariumhoof.bgfx4j.wip;

// todo
public class TextureCube extends Texture {

    protected TextureCube(short handle) {
        super(handle);
    }

    // todo replace BGFXMemory with api-specific reference
//    public void update(@NotNull BGFXMemory textureUpdateInfo, int layer, int mip, int x, int y, int width, int height) {
//        bgfx_update_texture_cube(handle, layer, mip, x, y, width, height, textureUpdateInf);
//    }
}
